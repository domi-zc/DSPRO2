services:
  fast-api-app:
    build: .
    ports:
      - "80:8000"
    volumes:
      - .:/app
    environment:
      - ENVIRONMENT=development
    command: ["fastapi", "dev", "frontend/main.py", "--host", "0.0.0.0", "--port", "8000"]